# File di configurazioni model_id V1.0# Righe che iniziano con # sono etichette e spiegazioni# Non aggiungere/rimuovere righe# Il software legge questo file per definire i dati di input di una# serie di modelli del tipo: y(t)=f(y(t-1),...,y(t-na),u(t), ... u(t-m))# dove y (variabile dipendente) è uno scalare # mentre u (variabile esogena) può essere un vettore# per indicazioni sul formato dei file di input vedere il file# formato_IO.txt (che farò, promesso)# ## PARTE 0: NOME TEST# nome test (verrà usato nei nomi dei file per identificarli)TEST## PARTE 1: DEFINIZIONE FILE DI INPUT# File csv variabile dipendente y (identificazione)./dati/dataset_IT2072A_id.csv# File csv variabile dipendente y (validazione, può essere vuoto)./dati/dataset_IT2072A_val.csv# Colonna dati y (attenzione! le colonne iniziano con la colonna 0)1# Colonna asse X file csv variabile y (serve solo per la scrittura dei file in uscita, può essere vuoto, in tal caso in uscita il file avrà un progressivo)# File csv variabili esogene u (identicazione, può essere vuoto, se non ci sono variabili esogene)./dati/dataset_emissioni_id_finale.csv# File csv variabili esogene u (validazione, può essere vuoto, se non ci sono variabili esogene)./dati/dataset_emissioni_val_finale.csv# Colonne dati u (attenzione! le colonne iniziano con la colonna 0. se ci sono più colonne di input, separarle con ,)0,1,2### PARTE 2: struttura dei modelli# Ordine parte autoregressiva na2# Ordine parti esogene (un valore per ogni variabile esogena, separato da virgole)1,1,1# Ritardo parti esogene (un valore per ogni variabile esogena, separato da virgole)0,0,0### PARTE 3: nome file di uscita./prova.txt